---

---

<section id="hero">
	<div class="overlay">
		<div class="hero-content">
			<h1>Transform your property with expert detailing by Supreme Detailing</h1>
			<p>Experience ultimate shine with professional detailing and pressure washing services. Supreme Detailing brings your property to life and ensures they look their best.</p>
			<div class="hero-buttons">
				<a href="#services" class="btn primary">View offered Services</a>
				<a href="#contact" class="btn secondary">Book Now</a>
			</div>
		</div>
	</div>

</section>

<style>
	#hero {
		background-image: url(../../images/erik-mclean-u7B2HTbzVko-unsplash.jpg);
		background-size: cover;
		background-position: center;
		background-repeat: no-repeat;
		min-height: 80vh;
		padding: 4rem 1rem;
		text-align: center;
		background-color: var(--background-color);
		color: var(--text-color);
		display: flex;
		align-items: center;
		justify-content: center;
		position: relative;
	}

	.hero-content {
		text-align: center;
		max-width: 900px;
	}

	.overlay {
		background-color: var(--overlay);
		backdrop-filter: blur(10px);
		-webkit-backdrop-filter: blur(10px);
		border: 1px solid rgba(255, 255, 255, 0.15);
		border-radius: 1rem;
		padding: 2rem;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
		width: 100%;
		height: 100%;
		display: flex;
		align-items: center;
		justify-content: center;
		animation: fadeInUp 0.8s ease-out both;
	}

	h1 {
		font-size: 2rem;
		margin-bottom: 1rem;
	}

	p {
		font-size: 1rem;
		margin-bottom: 2rem;
		line-height: 1.5;
	}

	.hero-buttons {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.btn {
		padding: 0.75rem 1.5rem;
		border: none;
		text-decoration: none;
		border-radius: 6px;
		font-weiht: 600;
		font-size: 1rem;
		transition: background 0.3s ease;

	}

	.btn.primary {
		background-color: var(--nav-bg);
		color: whitesmoke;
	}

	.btn.secondary {
		background-color: transparent;
		color: white;
		border: 2px solid var(--nav-bg);
	}

	@keyframes fadeInUp {
		from {
			opacity: 0;
			transform: translateY(20px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}
</style>
